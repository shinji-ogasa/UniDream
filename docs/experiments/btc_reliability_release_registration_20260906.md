# BTC Perp reliability: fixed production fit and export

Written before real production fitting. This registration, the new release source/tests/config, selection manifest and data-only preflight must be committed and pushed before execution. The release is `btc-perp-reliability-20260906`, recipe `perp_delay0_reliability_utility_risk1`. Root's bound [selection record](btc_accuracy_selection_20260906.json) chooses it using the stated, finite development comparison universe and deterministic rule. It does not rewrite formal P1 or prior experiment locks.

## Fixed fitting chronology

Use calendar25, anchored at **2026-07-16 13:45 UTC**. Training T is [2024-07-16,2026-01-16), scale S is [2026-01-16,2026-04-16), interval I is [2026-04-16,2026-07-16), all with the same 13:45 UTC boundary. The nominal E quarter ends2026-10-16, but **no E market rows enter the fitting dataset, no E predictions or outcomes are scored, and no new backtest or strategy targets are computed**. The raw semantic cutoff is strictly before E start. The inherited Spot loader decodes the complete bound Parquet before truncation; this is not a claim that later bytes were never decoded.

Reconstruct exactly the frozen Stage11 support: finite intersection of all trailing-variance columns, the original flow feature block, every original derivative feature group, and Technical plus UM flow delays0/1/4. The selected models consume Technical29 and Perp31; the other dependencies remain availability gates. No missing-value filling, compressed time grid or support reduction is permitted. Features at decision t use complete bars through t−1, shifted exactly once. UM delays are extra shifts of already shifted UM flow, not changes to Spot timing. Raw sources and their checksummed archive ledger/availability sidecars are bound by the existing additional-window data manifest.

Use six-hour UTC origins and the existing h24 outcome definition: enter at open[t+1], forecast log(close[t+24]/open[t+1]) with all24 future bars available. T/S/I labels must mature at t+375minutes strictly before their respective segment boundary. Original `calendar`/`segment_masks` helpers enforce chronology. Minimum T/S/I counts are512/64/64. Inference, score and scheduled E masks must all be empty. Input preflight may compute these masks and hash selected features/outcomes but must not fit coefficients, reliability weights, predictions, losses or orders.

## Exactly three base fits and one reliability fit

Call unchanged `fit_frozen_forecasts` once: Technical29 StandardScaler+Ridge(alpha100), Perp31 StandardScaler+Ridge(alpha100), and Technical29 HistGradientBoostingRegressor for log future realized variance. The HGB settings stay max_iter100,max_leaf_nodes7,min_samples_leaf64,learning_rate0.04,l2_regularization10,early_stoppingfalse,seed20260905. Unweighted scalers fit T only; both means fit T returns and variance fits T realized variance. S determines original return biases and the common variance multiplier. The inherited I quantiles are retained as provenance only; they are not advertised as uncertainty intervals for the changed Perp reliability mean.

For Perp, p=raw+saved_S_bias and a=exact saved S return mean from inherited `math.fsum(value/n)` arithmetic. Call unchanged `fit_reliability` once on S: d=p−a,r=y−a,B=mean(d²),C=mean(d*r),w=0 if B==0 or C<=0,w=1 if C>=B,otherwise C/B. No I value enters w. Use exact endpoint copies at0/1 and w*p+(1−w)*a otherwise. Risk is max(exp(clip(HGB_logvar,log1e−12,0))*saved_variance_multiplier,1e−12). No new model structure, candidate, cost, horizon, coefficient grid or calibration window is selected.

The run performs exactly3 base model fits and1 reliability calibration. An exclusive registration file is written before fitting; partial failures remain failures, with no automatic retry or alternative parameters. The config and all executable dependencies must match the current Git commit. Parent starts the actual run only after the commit is pushed. Numerical warnings are retained in the run log. Require finite selected predictions, independent scalar Ridge prediction differences≤1e−12 on every selected prediction row, and exact predictions after serializing and reloading all three models. These checks do not prove future profitability.

## Exported artifact contract

`codex_outputs/btc_reliability_release_v1/bundle` contains `models/{technical_mean,perp_delay0_mean,technical_variance}.joblib`, exact `calibration.json`, exact `reliability.json`, a numeric `prediction_fixture.npz`, and `manifest.json`. The fixture contains selected S/I feature matrices, raw predictions and final mu/variance, but no outcomes. It proves model/prediction arithmetic, not live action/accounting parity. Full selected T/predict inputs and masked calibration outcomes remain outside the bundle in `selected_inputs.npz`; `fit_provenance.json` binds their hashes and model procedure. There are no new score, forecast performance or economic path artifacts.

Manifest schema_version1 binds bundle/candidate IDs, BTCUSDT15m, production cutoff/calendar, exact ordered29/31 feature columns, model and execution parameters, separate feature/execution contract digests, source/runtime/raw-data hashes, files mapping non-manifest relative paths to their SHA256, selection evidence and fixture. The manifest is not self-hashed; its byte SHA is the released bundle SHA. Python3.12, NumPy2.2.6,pandas2.3.3,scikit-learn1.8.0 and actual joblib version are recorded.

Historical validation5–12 means are copied from the bound selection manifest and clearly labeled **recipe evidence, not the performance of these newly fitted production weights**. `research_scores_apply_to_production_weights`, `high_probability_generalization_established`, `rl_qualified`, `live_performance_claimed` and historical receipt proof remain false. Archive checksums are not historical receipt timestamps. This is ML plus a deterministic inventory controller, not a learned RL policy.

## Live execution retained by the receiving runtime

Use own normalized cash/units, initial cash0,initial equity1,units1/initial_open and common B&H inventory. Human display capital is separately10,000USDT. UTC decision hours0/6/12/18, exact next15m-open fill, fee0.00055,annual borrow0.10,BARS_YEAR35040,step0.08,deadband0.01,utility risk1/costbudget2,intent bounds0.5–1.12. Preserve natural exposure drift outside intent bounds. Missing forecast/current open gives hold; no retroactive or rolled-forward missed fill. Research export does not execute these actions; HF/Edge integration must independently verify model/feature/decision/accounting parity and receipt deadlines before changing the live demo. This registration authorizes no exchange orders or paid infrastructure.

Production cadence and live receipt checks do not turn past support into prospective evidence. New live values and normalized B&H comparisons must remain separate from historical validation mean metrics. Additional test outcomes do not change the fixed candidate or its fitting procedure.
